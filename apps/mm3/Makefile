#COMPONENT=SharedResourceDemoAppC
COMPONENT=mm3AppC
#PFLAGS += -DUSE_SPI_PACKET -DENABLE_SPI1_DMA
#PFLAGS += -DTEST_GPS -DLOOP_GPS_PWR
#PFLAGS += -DTEST_GPS
PFLAGS += -DTEST_GPS_BYTE

include $(MAKERULES)
